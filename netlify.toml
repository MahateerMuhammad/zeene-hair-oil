[build]
  command = "npm run build"
  publish = ".next"
  install = "npm ci --prefer-offline --no-audit --progress=false --legacy-peer-deps"

[build.environment]
  NODE_VERSION = "20.19.6"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build]
  command = "npm run build 2>&1 | grep -v \"'var' is not recognized\" || true"
  publish = ".next"

[build.environment]
  NPM_FLAGS = "--legacy-peer-deps"
  NODE_VERSION = "20.19.6"
  CI = "false"

[[plugins]]
  package = "@netlify/plugin-nextjs"
